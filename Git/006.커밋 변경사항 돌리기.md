# 006.커밋 변경사항 돌리기
```bash
# 마지막 커밋 수정 (커밋 메시지 포함)
$git commit --amend

# 마지막 커밋 되돌리기 (히스토리에서 제거됨)
$git reset --soft HEAD~1

# 이전 커밋으로 되돌리기 (변경 사항 유지)
$git reset --mixed HEAD~1

# 이전 커밋으로 되돌리기 (변경 사항도 제거)
$git reset --hard HEAD~1
```